scouts:
  web_scrapers:
    allowed_sources:
      - "google.com"
      - "duckduckgo.com"
    denied_sources:
      - "*.onion"
    # Job-specific: only linkedin.com/jobs is allowed.
    # Enforced by url_validator.py regex, not by this policy file.
    job_sources:
      - "linkedin.com"
