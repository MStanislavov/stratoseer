scouts:
  web_scrapers:
    allowed_sources:
      - "google.com"
      - "duckduckgo.com"
    denied_sources:
      - "*.onion"
    # Job-specific: only linkedin.com/jobs is allowed.
    # Enforced by web_scraper.py URL pattern checks, not by this policy file.
    job_sources:
      - "linkedin.com"
